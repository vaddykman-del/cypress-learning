config:
  target: "https://httpbin.io/"
  phases:
    - duration: 1
      arrivalRate: 1
      name: Artillery First Test
scenarios:
  - name: Getting a resource
    flow:
      - get:
          url: "{{ target }}get"
